type: object
required:
  - inn
  - ogrn
  - focusHref
  - licenses
allOf:
  - $ref: '#/definitions/KonturFocusResponse'
  - type: object
    properties:
      inn:
        type: string
      ogrn:
        type: string
      focusHref:
        $ref: '#/definitions/KonturFocusUrl'
      licenses:
        type: array
        items:
          $ref: '#/definitions/KonturFocusLicense'

