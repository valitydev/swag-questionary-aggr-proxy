type: object
allOf:
  - $ref: '#/definitions/EgrDetailsContractor'
  - type: object
    properties:
      okpo:
        type: string
      pfrRegNumber:
        type: string
      fssRegNumber:
        type: string
      fomsRegNumber:
        type: string
      okato:
        type: string
      shortenedAddress:
        $ref: '#/definitions/ShortenedAddress'
      activities:
        $ref: '#/definitions/Activity'
      regInfo:
        $ref: '#/definitions/RegInfo'
      nalogRegBody:
        $ref: '#/definitions/NalogRegBody'
      regBody:
        $ref: '#/definitions/RegBody'
      egrRecords:
        type: array
        items:
          $ref: '#/definitions/EgrRecord'
